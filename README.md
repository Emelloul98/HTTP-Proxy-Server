# HTTP-Proxy-Server
A proxy server implemented in C that handles HTTP requests, reads from and writes to sockets, creates directories, checks file existence, and performs file operations like writing, reading, and printing.
